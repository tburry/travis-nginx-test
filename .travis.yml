language: php

php:
    - 5.6

sudo: false

matrix:
  fast_finish: true

cache:
  - apt

addons:
  apt:
    packages:
      - nginx
      - realpath
  hosts:
      - localhost
      - test.dev

install:
#  - composer install
  - travis/install-nginx.sh

script:
  - curl -vs 'http://localhost:8080/hello.php' 2>&1